<?php

const DB_HOST = 'nome_host'; //può essere un nome DNS o un indirizzo IP

const DB_NAME = 'nome_db';

const DB_USER = 'nome_user';

const DB_PASS = 'password';

return [
    'DB_HOST' => DB_HOST,
    'DB_NAME' => DB_NAME,
    'DB_USER' => DB_USER,
    'DB_PASS' => DB_PASS,
    'BASEPATH' => BASEPATH,
];